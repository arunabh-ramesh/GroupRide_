# Hack RPI 2025

To run the NPM prototype, use the following commands given nodeJS and NPM are installed on the system. The commands for the server and the client need to be run in different terminals.

For the server:

```bash
cd server
npm instal
npm start
```

For the client:

```bash
cd client
npm install
npm run dev
```
